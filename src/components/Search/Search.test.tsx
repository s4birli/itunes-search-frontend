import { render } from '@testing-library/react';
import Search from './Search';

describe('<Search />', () => {
  /*it('should render', () => {
    const { container } = render(<Search onKeyDown={function (): void {
      throw new Error('Function not implemented.');
    } } term={''} />);
    expect(container).toMatchSnapshot();
  });*/
});
